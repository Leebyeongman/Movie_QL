type Movie {
    id: Int!
    name: String!
    score: Int!
}

type Query {
  movies: [Movie]!
  movie(id: Int!): Movie
}

// Mutation은 Database 상태가 변할 때 사용되는 거
type Mutation {
    addMovie(name: String!, score: Int!): Movie!
}